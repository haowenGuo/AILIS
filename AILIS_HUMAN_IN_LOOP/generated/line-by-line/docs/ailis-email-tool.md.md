# docs/ailis-email-tool.md 逐行讲解

- 快照提交：`a1f34b0c24bbf5fce436670e26ced97da3cb72c7`
- 文件职责：邮件工具：在账户与审批边界内读取或发送邮件。
- 文件类型：`documentation`
- 原始行数：147
- SHA-256：`7aee35bcb281d7bd8448f1b73d2f20948f7af03efc0c5ab0d24e68412e879ae3`
- 可运行副本：[打开源文件](../../../source/docs/ailis-email-tool.md)
- 依赖：未从静态文本识别到显式依赖
- 主要符号：未从静态文本识别到命名符号

> 阅读方法：先理解文件职责与依赖，再按行阅读；涉及异步、权限、记忆、工具和外部 I/O 的行应继续追踪调用方、错误路径和测试。

| 行号 | 原代码 | 逐行说明 |
| ---: | --- | --- |
| 1 | <code># AILIS Email Tool</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“邮件工具：在账户与审批边界内读取或发送邮件。”这一文件职责。 |
| 2 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 3 | <code>AILIS 自带一个本地 `email` 工具，用 IMAP 读取邮箱、用 SMTP 发送邮件。它不是 OpenClaw 的 `message` channel，而是适合个人桌面助手的轻量邮箱管理基座。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 4 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 5 | <code>## 支持范围</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“邮件工具：在账户与审批边界内读取或发送邮件。”这一文件职责。 |
| 6 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 7 | <code>第一版内置 3 类邮箱预设：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 8 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 9 | <code>&#124; Provider &#124; IMAP &#124; SMTP &#124; 密钥类型 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 10 | <code>&#124; --- &#124; --- &#124; --- &#124; --- &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 11 | <code>&#124; `qq` &#124; `imap.qq.com:993` &#124; `smtp.qq.com:465` &#124; QQ 邮箱授权码 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 12 | <code>&#124; `gmail` &#124; `imap.gmail.com:993` &#124; `smtp.gmail.com:465` &#124; App Password 或 OAuth2 access token &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 13 | <code>&#124; `outlook` &#124; `outlook.office365.com:993` &#124; `smtp.office365.com:587` &#124; OAuth2 access token，账号允许时可用应用专用密码 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 14 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 15 | <code>重要：不要把网页登录密码直接发给 Agent。QQ 通常用授权码；Gmail 推荐 OAuth2，也支持 App Password；Outlook/Microsoft 365 推荐 OAuth2，很多组织账号已经禁用 Basic Auth。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 16 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 17 | <code>## 工具接口</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“邮件工具：在账户与审批边界内读取或发送邮件。”这一文件职责。 |
| 18 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 19 | <code>统一工具名：`email`</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 20 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 21 | <code>动作：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 22 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 23 | <code>- `providers`：列出支持的邮箱预设</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 24 | <code>- `list` / `search`：读取邮件列表</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 25 | <code>- `read`：按 IMAP UID 读取邮件正文和附件元数据</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 26 | <code>- `draft`：生成邮件草稿，不发出</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 27 | <code>- `send`：发送邮件，需要审批</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 28 | <code>- `mark_read` / `mark_unread`：修改已读状态，需要审批</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 29 | <code>- `move`：移动邮件，需要审批</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 30 | <code>- `delete`：删除邮件，需要审批</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 31 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 32 | <code>## 通过 Gateway 调用</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“邮件工具：在账户与审批边界内读取或发送邮件。”这一文件职责。 |
| 33 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 34 | <code>列出 Provider：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 35 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 36 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 37 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 38 | <code>  "tool": "email",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 39 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 40 | <code>    "action": "providers"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 41 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 42 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 43 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 44 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 45 | <code>查看 QQ 收件箱：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 46 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 47 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 48 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 49 | <code>  "tool": "email",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 50 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 51 | <code>    "action": "list",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 52 | <code>    "provider": "qq",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 53 | <code>    "account": "you@qq.com",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 54 | <code>    "secret": "QQ邮箱授权码",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 55 | <code>    "limit": 10</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 56 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 57 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 58 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 59 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 60 | <code>读取某封邮件：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 61 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 62 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 63 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 64 | <code>  "tool": "email",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 65 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 66 | <code>    "action": "read",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 67 | <code>    "provider": "gmail",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 68 | <code>    "account": "you@gmail.com",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 69 | <code>    "secret": "Google App Password 或 OAuth2 token",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 70 | <code>    "uid": 123</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 71 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 72 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 73 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 74 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 75 | <code>草拟邮件：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 76 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 77 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 78 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 79 | <code>  "tool": "email",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 80 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 81 | <code>    "action": "draft",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 82 | <code>    "to": "friend@example.com",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 83 | <code>    "subject": "周报",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 84 | <code>    "text": "这是草稿内容"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 85 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 86 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 87 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 88 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 89 | <code>发送邮件：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 90 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 91 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 92 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 93 | <code>  "tool": "email",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 94 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 95 | <code>    "action": "send",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 96 | <code>    "provider": "outlook",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 97 | <code>    "account": "you@outlook.com",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 98 | <code>    "secret": "OAuth2 access token 或应用专用密码",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 99 | <code>    "to": "friend@example.com",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 100 | <code>    "subject": "你好",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 101 | <code>    "text": "正文"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 102 | <code>  },</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 103 | <code>  "context": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 104 | <code>    "approved": true</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 105 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 106 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 107 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 108 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 109 | <code>没有 `context.approved=true` 时，`send/delete/move/mark_*` 会返回 `needs_approval`。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 110 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 111 | <code>## 环境变量</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“邮件工具：在账户与审批边界内读取或发送邮件。”这一文件职责。 |
| 112 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 113 | <code>为了避免把密钥写进聊天记录，建议先用环境变量：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 114 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 115 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 116 | <code>$env:AILIS_EMAIL_QQ_ACCOUNT="you@qq.com"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 117 | <code>$env:AILIS_EMAIL_QQ_SECRET="QQ邮箱授权码"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 118 | <code>$env:AILIS_EMAIL_GMAIL_ACCOUNT="you@gmail.com"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 119 | <code>$env:AILIS_EMAIL_GMAIL_SECRET="Google App Password 或 OAuth2 token"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 120 | <code>$env:AILIS_EMAIL_OUTLOOK_ACCOUNT="you@outlook.com"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 121 | <code>$env:AILIS_EMAIL_OUTLOOK_SECRET="OAuth2 access token 或应用专用密码"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 122 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 123 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 124 | <code>之后 Agent 里可以直接说：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 125 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 126 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 127 | <code>查看今天的邮件</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 128 | <code>查看未读邮件</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 129 | <code>读取邮件 123</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 130 | <code>草拟邮件给 friend@example.com 主题 周报 内容 今天进展如下...</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 131 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 132 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 133 | <code>## 安全策略</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“邮件工具：在账户与审批边界内读取或发送邮件。”这一文件职责。 |
| 134 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 135 | <code>- Gateway 审计会脱敏 `secret/password/token/pass/authCode` 等字段。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 136 | <code>- 邮件发送、删除、移动、标记已读/未读都需要审批。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 137 | <code>- 草拟邮件不会触网，也不需要密钥。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 138 | <code>- 第一版不保存邮箱密钥；桌面控制面板后续可以接系统密钥库或 Electron `safeStorage`。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 139 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 140 | <code>## 官方接入资料</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“邮件工具：在账户与审批边界内读取或发送邮件。”这一文件职责。 |
| 141 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 142 | <code>- Gmail IMAP/SMTP: https://support.google.com/mail/answer/7126229</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 143 | <code>- Gmail API / OAuth: https://developers.google.com/gmail/api/auth/about-auth</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 144 | <code>- Microsoft IMAP/POP/SMTP OAuth: https://learn.microsoft.com/en-us/exchange/client-developer/legacy-protocols/how-to-authenticate-an-imap-pop-smtp-application-by-using-oauth</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 145 | <code>- QQ 邮箱授权码获取参考: https://consumer.huawei.com/cn/support/content/zh-cn15872097/</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 146 | <code>- Nodemailer SMTP client: https://nodemailer.com/smtp/</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 147 | <code>- ImapFlow IMAP client: https://imapflow.com/</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
