# docs/ailis-humanlike-eval.md 逐行讲解

- 快照提交：`a1f34b0c24bbf5fce436670e26ced97da3cb72c7`
- 文件职责：设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。
- 文件类型：`documentation`
- 原始行数：252
- SHA-256：`b2c5e2b8d0512665a5d7cdafcfe3a3ed1c10f984c48cbcc64b5bce265f412d7d`
- 可运行副本：[打开源文件](../../../source/docs/ailis-humanlike-eval.md)
- 依赖：未从静态文本识别到显式依赖
- 主要符号：未从静态文本识别到命名符号

> 阅读方法：先理解文件职责与依赖，再按行阅读；涉及异步、权限、记忆、工具和外部 I/O 的行应继续追踪调用方、错误路径和测试。

| 行号 | 原代码 | 逐行说明 |
| ---: | --- | --- |
| 1 | <code># AILIS Humanlike Experience Eval</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 2 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 3 | <code>这套 Eval 用来评估 AILIS 是否像一个长期陪伴型私人助手，而不是只评估回答对错。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 4 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 5 | <code>## 评估对象</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 6 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 7 | <code>- 普通对话回复文本。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 8 | <code>- 语音文本、TTS 风格、表情、动作、气泡文字。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 9 | <code>- 视觉理解后的自然回复。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 10 | <code>- 记忆和好感度对表达的影响。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 11 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 12 | <code>## 核心指标</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 13 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 14 | <code>&#124; 指标 &#124; 含义 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 15 | <code>&#124; --- &#124; --- &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 16 | <code>&#124; `persona_consistency` &#124; 人设一致性，是否一直像 AILIS &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 17 | <code>&#124; `naturalness` &#124; 语气自然度，避免过度卖萌、过度解释、过度工具化 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 18 | <code>&#124; `memory_usefulness` &#124; 记忆使用质量，合理使用偏好，不暴露内部好感度 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 19 | <code>&#124; `emotional_fit` &#124; 情绪响应，疲惫、烦躁、开心、求助时是否合适 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 20 | <code>&#124; `multimodal_sync` &#124; 多模态同步感，语音、表情、动作、口唇、气泡是否一致 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 21 | <code>&#124; `low_tool_feeling` &#124; 低工具感，用户是否感觉是 AILIS 在帮忙 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 22 | <code>&#124; `relationship_stage_fit` &#124; 关系阶段匹配度，亲密感是否符合好感度 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 23 | <code>&#124; `task_completion` &#124; 任务完成能力，从用户角度看当前请求是否被完成、推进，或诚实说明卡点和下一步 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 24 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 25 | <code>## 好感度规则</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 26 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 27 | <code>&#124; 好感度 &#124; 期望 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 28 | <code>&#124; --- &#124; --- &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 29 | <code>&#124; 40-60 &#124; 温和、熟悉但不过分亲密 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 30 | <code>&#124; 61-79 &#124; 更熟悉、更自然、更有陪伴感 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 31 | <code>&#124; 80-100 &#124; 允许明显亲密、主动、轻微撒娇、更多默契表达 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 32 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 33 | <code>任何阶段都不能影响安全、隐私、事实准确性、工具审批和基础帮助质量。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 34 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 35 | <code>## 数据集</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 36 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 37 | <code>种子集在：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 38 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 39 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 40 | <code>evals/ailis-humanlike/scenarios.jsonl</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 41 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 42 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 43 | <code>1000 条正式数据集的覆盖规划在：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 44 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 45 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 46 | <code>evals/ailis-humanlike/dataset-plan.json</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 47 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 48 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 49 | <code>每条样例包含：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 50 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 51 | <code>- `id`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 52 | <code>- `category`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 53 | <code>- `affinity_score`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 54 | <code>- `user_message`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 55 | <code>- `memory_context`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 56 | <code>- `expected_behavior`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 57 | <code>- `anti_patterns`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 58 | <code>- `modalities`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 59 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 60 | <code>后续扩到 1000 条时，建议按这些桶采样：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 61 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 62 | <code>- 情绪陪伴</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 63 | <code>- 长期记忆</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 64 | <code>- 人设一致性</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 65 | <code>- 低工具感</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 66 | <code>- 视觉理解</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 67 | <code>- 多模态同步</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 68 | <code>- 关系阶段匹配</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 69 | <code>- 任务开场体验</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 70 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 71 | <code>目标分布：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 72 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 73 | <code>&#124; 类别 &#124; 目标数量 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 74 | <code>&#124; --- &#124; ---: &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 75 | <code>&#124; `emotional_response` &#124; 160 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 76 | <code>&#124; `memory_use` &#124; 150 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 77 | <code>&#124; `relationship_stage` &#124; 140 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 78 | <code>&#124; `low_tool_feeling` &#124; 130 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 79 | <code>&#124; `multimodal_sync` &#124; 120 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 80 | <code>&#124; `vision_experience` &#124; 100 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 81 | <code>&#124; `task_helpfulness` &#124; 100 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 82 | <code>&#124; `correction` &#124; 60 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 83 | <code>&#124; `safety_privacy_boundary` &#124; 40 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 84 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 85 | <code>好感度覆盖：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 86 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 87 | <code>&#124; 好感度 &#124; 目标比例 &#124; 体验要求 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 88 | <code>&#124; --- &#124; ---: &#124; --- &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 89 | <code>&#124; 0-39 &#124; 10% &#124; 克制、认真、快速修正 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 90 | <code>&#124; 40-60 &#124; 35% &#124; 温和、熟悉但不过分亲密 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 91 | <code>&#124; 61-79 &#124; 30% &#124; 更熟悉、更自然、更有陪伴感 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 92 | <code>&#124; 80-100 &#124; 25% &#124; 允许明显亲密、主动、轻微撒娇、更多默契表达 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 93 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 94 | <code>每条样例都必须有 `expected_behavior` 和 `anti_patterns`。这是 Judge 稳定性的核心：同一个候选回复被重复评估时，Judge 应优先对齐这些显式标准，而不是按个人偏好漂移。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 95 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 96 | <code>## Judge 规则</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 97 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 98 | <code>规则实现位置：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 99 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 100 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 101 | <code>electron/ailis-humanlike-eval.cjs</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 102 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 103 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 104 | <code>核心原则：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 105 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 106 | <code>- Judge 只根据 `scenario`、`candidate_response` 和 rubric 打分。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 107 | <code>- 每个指标 1-5 分，最后按权重汇总成 0-100。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 108 | <code>- Judge 的主视角是用户体验，不是 Agent Loop 调试。工具执行摘要只作为事实证据，用来判断“我看过/做完了”这类说法是否有依据。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 109 | <code>- 好感度 80-100 的明显亲密、主动、轻微撒娇是产品目标，不应被默认扣分。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 110 | <code>- 任何阶段都不能牺牲安全、隐私、事实准确性和工具审批。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 111 | <code>- `task_completion` 不是只看工具是否成功。普通陪伴请求也要判断是否真的安抚、总结、建议或收束；复杂任务则看是否完成、推进、请求确认或诚实停住。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 112 | <code>- 如果回复暴露内部好感度、memory_context、tool_call、raw observation，触发硬失败。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 113 | <code>- 如果视觉或记忆内容没有依据却说得很确定，触发硬失败或重扣。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 114 | <code>- 如果没有提供语音、表情、动作等信息，`multimodal_sync` 最多 3 分，除非场景不涉及多模态。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 115 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 116 | <code>## 由当前 Codex 会话担任强 Judge</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 117 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 118 | <code>当前桌面会话里的 Codex 可以作为强 Judge 做人工确认级评估，但工程上不能假装本地脚本能直接调用“这个聊天窗口里的我”。可靠做法是使用 Judge Packet：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 119 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 120 | <code>1. Runner 导出标准 Judge Packet。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 121 | <code>2. 把 packet 交给当前 Codex 会话评审。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 122 | <code>3. Codex 只返回一行 JSON judgment，不输出解释性 Markdown。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 123 | <code>4. Runner 再导入 JSONL judgment，生成统一 summary。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 124 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 125 | <code>导出 Judge Packet：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 126 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 127 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 128 | <code>pnpm eval:ailis-humanlike -- --responses evals/ailis-humanlike/example-responses.jsonl --export-judge-packets --limit 1</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 129 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 130 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 131 | <code>把输出的 `.judge-packets.jsonl` 交给强 Judge 后，保存为一行一个 JSON 对象的文件，例如：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 132 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 133 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 134 | <code>eval-results/ailis-humanlike/manual-judgments.jsonl</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 135 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 136 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 137 | <code>再导入汇总：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 138 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 139 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 140 | <code>pnpm eval:ailis-humanlike -- --responses evals/ailis-humanlike/example-responses.jsonl --judgments eval-results/ailis-humanlike/manual-judgments.jsonl</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 141 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 142 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 143 | <code>这样每次评估都留下三类证据：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 144 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 145 | <code>- 输入：scenario + candidate response + Judge Packet</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 146 | <code>- 输出：Judge 返回的 JSON judgment</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 147 | <code>- 汇总：Runner 生成的 result JSONL 和 summary JSON</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 148 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 149 | <code>如果以后接 API Judge，则仍走同一套 rubric 和 parser，只是由 Runner 直接调用模型。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 150 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 151 | <code>## 运行</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 152 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 153 | <code>生成 1000 条正式场景：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 154 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 155 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 156 | <code>pnpm eval:ailis-humanlike:generate</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 157 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 158 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 159 | <code>生成覆盖分析报告：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 160 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 161 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 162 | <code>pnpm eval:ailis-humanlike:report</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 163 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 164 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 165 | <code>只校验数据集：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 166 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 167 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 168 | <code>pnpm eval:ailis-humanlike:validate</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 169 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 170 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 171 | <code>生成并校验 30 天长程陪伴基准：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 172 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 173 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 174 | <code>pnpm eval:ailis-humanlike:longitudinal:generate</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 175 | <code>pnpm eval:ailis-humanlike:longitudinal:validate</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 176 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 177 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 178 | <code>这套长程基准在：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 179 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 180 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 181 | <code>evals/ailis-humanlike/longitudinal-companionship-30d.dataset.json</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 182 | <code>evals/ailis-humanlike/longitudinal-companionship-30d.scenarios.jsonl</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 183 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 184 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 185 | <code>`.dataset.json` 是产品侧长程陪伴原始数据，结构是 `case -&gt; 第 N 天 -&gt; 用户对话 1/2/3...`，适合人工检查和继续扩写。`.scenarios.jsonl` 是从原始数据派生出的 Eval runner 格式。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 186 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 187 | <code>它包含 10 条长程样例，每条都是 30 天历史，每天 12 次用户对话，总计 3600 次用户输入。每天混合真实私人助手场景：情感陪伴、邮件、论文阅读、Word/表格脚本、GitHub 提交、视觉截图、ASR、TTS/口唇/气泡、多模态同步、Tool/MCP/Skill、审批与隐私、重启恢复和 max steps 表达。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 188 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 189 | <code>真实 Agent 长程评估建议先跑单条烟测：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 190 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 191 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 192 | <code>pnpm eval:ailis-humanlike:longitudinal:real -- --limit 1</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 193 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 194 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 195 | <code>确认模型上下文、超时和费用都可接受后，再跑完整 10 条：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 196 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 197 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 198 | <code>pnpm eval:ailis-humanlike:longitudinal:real</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 199 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 200 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 201 | <code>更严格的全流程长程评估使用 Evaluation Agent，不再只评每条 30 天历史的最后一句，而是抽取邮件、论文、Word/表格、GitHub、视觉、ASR/多模态、记忆、隐私审批、Tool/MCP/Skill 和月末总结等 checkpoint 逐个生成候选回复，再用 LLM-as-judge 按完整指标评分：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 202 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 203 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 204 | <code>pnpm eval:ailis-humanlike:longitudinal-agent:validate</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 205 | <code>pnpm eval:ailis-humanlike:longitudinal-agent:smoke</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 206 | <code>pnpm eval:ailis-humanlike:longitudinal-agent</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 207 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 208 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 209 | <code>默认 `checkpoint-mode=critical`，用于覆盖每条 case 的关键任务节点；`--checkpoint-mode daily` 会每天抽 3 个 turn；`--checkpoint-mode all` 会评估全部 3600 个用户 turn。默认 `history-mode=full`，会把当前 checkpoint 前的长程历史送给 Agent，真正测试上下文承压能力。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 210 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 211 | <code>评估已有回复：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 212 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 213 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 214 | <code>pnpm eval:ailis-humanlike -- --responses evals/ailis-humanlike/example-responses.jsonl --judge-base-url &lt;base&gt; --judge-model &lt;model&gt; --judge-api-key &lt;key&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 215 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 216 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 217 | <code>连接正在运行的 AILIS Gateway，生成真实 AILIS 回复后再评：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 218 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 219 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 220 | <code>pnpm eval:ailis-humanlike -- --generate-with-agent --gateway-url http://127.0.0.1:19777 --judge-base-url &lt;base&gt; --judge-model &lt;model&gt; --judge-api-key &lt;key&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 221 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 222 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 223 | <code>输出在：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 224 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 225 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 226 | <code>eval-results/ailis-humanlike/</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 227 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 228 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 229 | <code>原始 13 条种子样例保留在：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 230 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 231 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 232 | <code>evals/ailis-humanlike/scenarios.seed.jsonl</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 233 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 234 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 235 | <code>## 稳定性检查</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 236 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 237 | <code>建议每次改 Eval 后运行：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 238 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 239 | <code>```powershell</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 240 | <code>node --check electron\ailis-humanlike-eval.cjs</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 241 | <code>node --check scripts\run-ailis-humanlike-eval.mjs</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 242 | <code>pnpm test:ailis-humanlike-eval</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 243 | <code>pnpm eval:ailis-humanlike:validate</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 244 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 245 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 246 | <code>评估有效性不只看平均分，还要看：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 247 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 248 | <code>- `hard_fail_count` 是否为 0。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 249 | <code>- `by_category` 是否暴露某类体验短板。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 250 | <code>- `by_relationship_stage` 是否验证 40-60、61-79、80-100 三段亲密度差异。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 251 | <code>- `metric_averages.memory_usefulness` 是否因为乱用记忆下降。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 252 | <code>- `metric_averages.low_tool_feeling` 是否因为工具日志感下降。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
