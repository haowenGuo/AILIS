# docs/ailis-file-manager-tool.md 逐行讲解

- 快照提交：`a1f34b0c24bbf5fce436670e26ced97da3cb72c7`
- 文件职责：文件管理工具：受路径保护地读取、写入、移动或检查本地文件。
- 文件类型：`documentation`
- 原始行数：146
- SHA-256：`554f8e72b0549ce6e727996f920d2c626eb58fee5d4d81d97b9931a40c7d608a`
- 可运行副本：[打开源文件](../../../source/docs/ailis-file-manager-tool.md)
- 依赖：未从静态文本识别到显式依赖
- 主要符号：未从静态文本识别到命名符号

> 阅读方法：先理解文件职责与依赖，再按行阅读；涉及异步、权限、记忆、工具和外部 I/O 的行应继续追踪调用方、错误路径和测试。

| 行号 | 原代码 | 逐行说明 |
| ---: | --- | --- |
| 1 | <code># AILIS File Manager Tool</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“文件管理工具：受路径保护地读取、写入、移动或检查本地文件。”这一文件职责。 |
| 2 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 3 | <code>AILIS 自带一个本地 `file_manager` 工具，用来做文件整理、垃圾文件扫描、临时文件清理和安全的 C 盘清理计划。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 4 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 5 | <code>核心原则：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 6 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 7 | <code>- 默认只做 `dryRun` 计划，不直接移动或删除。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 8 | <code>- 真正执行 `clean` / `organize` 必须带 `context.approved=true`。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 9 | <code>- `clean` 默认使用 `quarantine` 隔离模式，把文件移动到隔离区，不永久删除。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 10 | <code>- 永久删除必须同时设置 `context.approved=true`、`allowPermanentDelete=true`、`dangerous=true`。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 11 | <code>- C 盘清理不扫描整个 `C:\`，只使用 `c_drive_safe` 安全预设。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 12 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 13 | <code>## 工具接口</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“文件管理工具：受路径保护地读取、写入、移动或检查本地文件。”这一文件职责。 |
| 14 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 15 | <code>统一工具名：`file_manager`</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 16 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 17 | <code>动作：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 18 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 19 | <code>- `schema`：查看工具能力、预设和安全策略</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 20 | <code>- `scan`：扫描候选垃圾文件</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 21 | <code>- `clean`：生成清理计划或执行隔离/删除</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 22 | <code>- `organize`：按文件类型生成整理计划或移动文件</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 23 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 24 | <code>## 安全预设</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“文件管理工具：受路径保护地读取、写入、移动或检查本地文件。”这一文件职责。 |
| 25 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 26 | <code>&#124; Profile &#124; 说明 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 27 | <code>&#124; --- &#124; --- &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 28 | <code>&#124; `workspace` &#124; 当前工作区 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 29 | <code>&#124; `downloads` &#124; 用户下载目录 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 30 | <code>&#124; `desktop` &#124; 用户桌面 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 31 | <code>&#124; `documents` &#124; 用户文档 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 32 | <code>&#124; `temp` &#124; 用户临时目录 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 33 | <code>&#124; `c_drive_safe` &#124; Windows 安全清理预设：用户临时目录、LocalAppData Temp、Windows Temp、下载目录 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 34 | <code>&#124; `windows_safe_cleanup` &#124; `c_drive_safe` 的别名 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 35 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 36 | <code>## Gateway 调用示例</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“文件管理工具：受路径保护地读取、写入、移动或检查本地文件。”这一文件职责。 |
| 37 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 38 | <code>查看工具 schema：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 39 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 40 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 41 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 42 | <code>  "tool": "file_manager",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 43 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 44 | <code>    "action": "schema"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 45 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 46 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 47 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 48 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 49 | <code>扫描下载目录：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 50 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 51 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 52 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 53 | <code>  "tool": "file_manager",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 54 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 55 | <code>    "action": "scan",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 56 | <code>    "profile": "downloads",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 57 | <code>    "minAgeDays": 7</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 58 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 59 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 60 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 61 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 62 | <code>生成 C 盘安全清理计划：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 63 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 64 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 65 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 66 | <code>  "tool": "file_manager",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 67 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 68 | <code>    "action": "clean",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 69 | <code>    "profile": "c_drive_safe",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 70 | <code>    "dryRun": true,</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 71 | <code>    "minAgeDays": 7</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 72 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 73 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 74 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 75 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 76 | <code>执行隔离清理：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 77 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 78 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 79 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 80 | <code>  "tool": "file_manager",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 81 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 82 | <code>    "action": "clean",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 83 | <code>    "profile": "c_drive_safe",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 84 | <code>    "dryRun": false,</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 85 | <code>    "mode": "quarantine"</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 86 | <code>  },</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 87 | <code>  "context": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 88 | <code>    "approved": true</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 89 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 90 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 91 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 92 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 93 | <code>整理下载目录：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 94 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 95 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 96 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 97 | <code>  "tool": "file_manager",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 98 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 99 | <code>    "action": "organize",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 100 | <code>    "profile": "downloads",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 101 | <code>    "dryRun": true</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 102 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 103 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 104 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 105 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 106 | <code>执行整理：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 107 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 108 | <code>```json</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 109 | <code>{</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 110 | <code>  "tool": "file_manager",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 111 | <code>  "args": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 112 | <code>    "action": "organize",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 113 | <code>    "profile": "downloads",</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 114 | <code>    "dryRun": false</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 115 | <code>  },</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 116 | <code>  "context": {</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 117 | <code>    "approved": true</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 118 | <code>  }</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 119 | <code>}</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 120 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 121 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 122 | <code>## Agent 自然语言</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“文件管理工具：受路径保护地读取、写入、移动或检查本地文件。”这一文件职责。 |
| 123 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 124 | <code>现在 Agent 可以识别：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 125 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 126 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 127 | <code>清理 C盘垃圾文件</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 128 | <code>扫描临时文件</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 129 | <code>清理下载目录垃圾</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 130 | <code>整理下载目录</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 131 | <code>整理桌面文件</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 132 | <code>分类当前工作区文件</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 133 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 134 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 135 | <code>这些自然语言默认都会先生成计划，不会直接删文件。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 136 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 137 | <code>## 第一版会识别的垃圾候选</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“文件管理工具：受路径保护地读取、写入、移动或检查本地文件。”这一文件职责。 |
| 138 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 139 | <code>- 临时/备份扩展名：`.tmp`、`.temp`、`.bak`、`.old`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 140 | <code>- 日志和崩溃文件：旧 `.log`、`.dmp`、`.dump`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 141 | <code>- 下载残留：`.crdownload`、`.part`、`.download`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 142 | <code>- 可选：空文件、生成目录和依赖目录，需要显式参数开启</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 143 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 144 | <code>## 边界</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“文件管理工具：受路径保护地读取、写入、移动或检查本地文件。”这一文件职责。 |
| 145 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 146 | <code>第一版不做注册表清理、不卸载软件、不清理驱动、不删除 Windows 更新缓存、不碰 `Program Files`。这些都属于高风险系统维护，应后续接 Windows 官方 API 或专门维护工具，并且必须有更强审批和回滚策略。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
