# docs/ailis-vs-codex-execution-benchmark.md 逐行讲解

- 快照提交：`a1f34b0c24bbf5fce436670e26ced97da3cb72c7`
- 文件职责：设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。
- 文件类型：`documentation`
- 原始行数：154
- SHA-256：`5400eef38f7c1ad72de69bff1eb0f58f4635dfc6c3803fb385a4ebdc5ac02e80`
- 可运行副本：[打开源文件](../../../source/docs/ailis-vs-codex-execution-benchmark.md)
- 依赖：未从静态文本识别到显式依赖
- 主要符号：未从静态文本识别到命名符号

> 阅读方法：先理解文件职责与依赖，再按行阅读；涉及异步、权限、记忆、工具和外部 I/O 的行应继续追踪调用方、错误路径和测试。

| 行号 | 原代码 | 逐行说明 |
| ---: | --- | --- |
| 1 | <code># AILIS vs Codex 执行能力实测</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 2 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 3 | <code>日期：2026-05-24  </code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 4 | <code>AILIS 命令：`pnpm ailis:benchmark-execution`</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 5 | <code>Codex CLI：`codex-cli 0.120.0`</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 6 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 7 | <code>## 测试任务</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 8 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 9 | <code>这轮没有只测 hello world，而是测了更接近任务执行智能体的场景：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 10 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 11 | <code>1. 代码修复：构造一个有 bug 的 Node 项目，先跑失败测试，再修 `src/math.js`，再跑测试确认通过。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 12 | <code>2. 长进程控制：启动一个持续输出的 Node 进程，读取 stdout，写入 stdin，让它退出。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 13 | <code>3. 安全策略：尝试越界读文件、未审批执行命令、只读权限下写文件。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 14 | <code>4. Codex 对照：用 Codex CLI 在临时目录里跑同类文件创建任务和代码修复任务。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 15 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 16 | <code>## AILIS 实测结果</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 17 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 18 | <code>命令：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 19 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 20 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 21 | <code>pnpm ailis:benchmark-execution</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 22 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 23 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 24 | <code>结果：通过。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 25 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 26 | <code>工作区：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 27 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 28 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 29 | <code>F:\temp\ailis-execution-bench-Va1bmh</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 30 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 31 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 32 | <code>关键结果：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 33 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 34 | <code>&#124; 项目 &#124; 结果 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 35 | <code>&#124;---&#124;---&#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 36 | <code>&#124; 代码修复 &#124; 通过。先测出失败，再写入修复，`npm test` 通过 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 37 | <code>&#124; 初始失败测试 &#124; `exec` 返回 `error`，耗时约 `2242ms` &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 38 | <code>&#124; 修复后测试 &#124; `exec` 返回 `completed`，耗时约 `2056ms` &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 39 | <code>&#124; code tool 测试 &#124; `code.test` 返回 `completed`，耗时约 `1853ms` &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 40 | <code>&#124; 语义索引 &#124; `code.semantic_index` 返回 `completed`，索引文件数 `3` &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 41 | <code>&#124; 长进程 &#124; `session_start`、`process_read`、`process_write`、最终 `process_read` 全部通过 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 42 | <code>&#124; 安全策略 &#124; 越界 read 被 `blocked`；未审批 exec 返回 `needs_approval`；只读 profile 写文件被 `blocked` &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 43 | <code>&#124; transcript &#124; `bench-code-repair` 有 `18` 个 transcript item，类型为 `tool.call` / `tool.result` &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 44 | <code>&#124; audit &#124; `17` 条 audit entries &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 45 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 46 | <code>本轮发现并修复的问题：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 47 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 48 | <code>- `permissionProfile: { fileSystem: "read-only" }` 原本没有挡住 `write`。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 49 | <code>- 原因是 `isReadOnlyProfile()` 只看 `profile.id &#124;&#124; profile.fileSystem`，默认 `id=workspace-write` 会盖住 `fileSystem`。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 50 | <code>- 已修为同时检查 `id` 和 `fileSystem`，并补了回归测试。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 51 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 52 | <code>## Codex 实测结果</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 53 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 54 | <code>### 1. 默认模型启动</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 55 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 56 | <code>命令形态：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 57 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 58 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 59 | <code>codex exec --skip-git-repo-check --full-auto --json -C &lt;tmp&gt; ...</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 60 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 61 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 62 | <code>结果：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 63 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 64 | <code>- 默认模型是 `gpt-5.5`。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 65 | <code>- 当前本机 CLI `codex-cli 0.120.0` 被服务端拒绝：</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 66 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 67 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 68 | <code>The 'gpt-5.5' model requires a newer version of Codex.</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 69 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 70 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 71 | <code>所以后续改用：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 72 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 73 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 74 | <code>codex exec --skip-git-repo-check --full-auto --json -m gpt-5.4 -C &lt;tmp&gt; ...</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 75 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 76 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 77 | <code>### 2. 简单文件创建任务</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 78 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 79 | <code>任务：创建 `result.txt`，写入 `CODEX_SMOKE_OK`，读回验证。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 80 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 81 | <code>工作区：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 82 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 83 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 84 | <code>F:\temp\codex-capability-f86f0e3329a34962bcea36ba1719d961</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 85 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 86 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 87 | <code>结果：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 88 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 89 | <code>- exit code：`0`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 90 | <code>- 文件创建成功：`result.txt = CODEX_SMOKE_OK`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 91 | <code>- 最终消息：`Verified by reading it back. Filename: result.txt.`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 92 | <code>- 有 `turn.completed`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 93 | <code>- 耗时约 `178s`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 94 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 95 | <code>观察：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 96 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 97 | <code>- 中间出现多次 `stream disconnected` / `Reconnecting...`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 98 | <code>- 最终 fallback 到 HTTP 后完成。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 99 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 100 | <code>### 3. 复杂代码修复任务</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 101 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 102 | <code>任务：同样构造一个失败的 Node 项目，让 Codex 修 `src/math.js` 并跑 `npm test`。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 103 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 104 | <code>工作区：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 105 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 106 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 107 | <code>F:\temp\codex-code-repair-872f05c92d7d45c1bcb2c379d5f13069</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 108 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 109 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 110 | <code>结果：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 111 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 112 | <code>- 外层命令超时：`421s`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 113 | <code>- 但任务中途已经把 `src/math.js` 修好</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 114 | <code>- 手动复核 `npm test --prefix &lt;tmp&gt;` 通过，输出 `CODEX_COMPLEX_TEST_OK`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 115 | <code>- 没有生成 `last-message.txt`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 116 | <code>- event log 里没有 `turn.completed`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 117 | <code>- 留下了临时文件 `src/math.new.js`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 118 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 119 | <code>Codex event 里出现的关键行为：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 120 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 121 | <code>- 先运行 `npm test`，发现失败。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 122 | <code>- 读取 `tests/run-tests.js` 和 `src/math.js`。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 123 | <code>- 判断出两个 bug：`sum` 只返回首项，`average([])` 除以 0。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 124 | <code>- 多次尝试 `apply_patch`，但 patch context 匹配失败。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 125 | <code>- 创建 `src/math.new.js` 成功。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 126 | <code>- `Move-Item` 覆盖原文件失败：`Access to the path is denied.`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 127 | <code>- 改用 `Set-Content` 覆盖 `src/math.js` 成功。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 128 | <code>- 再次运行 `npm test` 成功。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 129 | <code>- 尝试清理 `src/math.new.js` 失败，之后外层超时。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 130 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 131 | <code>## 对照结论</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 132 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 133 | <code>&#124; 维度 &#124; AILIS 当前表现 &#124; Codex 当前表现 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 134 | <code>&#124;---&#124;---&#124;---&#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 135 | <code>&#124; 文件/代码执行速度 &#124; 快，整个复杂 benchmark 约十几秒 &#124; 慢，本机实测简单任务约 178s，复杂任务超过 421s &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 136 | <code>&#124; 多步自主修复 &#124; 目前 benchmark 是工具链确定性执行，Agentic Loop 有基础但没有 Codex 那么强的自我诊断 &#124; 强，会读测试、定位 bug、尝试 patch、失败后换策略 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 137 | <code>&#124; Windows 文件操作鲁棒性 &#124; 本轮稳定；直接 `write` 覆盖成功 &#124; 遇到 patch context、move/delete 权限问题，最终代码修好但收尾卡住 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 138 | <code>&#124; 命令执行 &#124; `exec`、`session_start`、`process_read/write/kill` 实测通过 &#124; `command_execution` 实测可用；源码层有更完整事件协议 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 139 | <code>&#124; 长进程控制 &#124; 实测通过，能读 stdout 和写 stdin &#124; 本轮未跑同款任务；源码/CLI 支持 process/spawn 类能力 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 140 | <code>&#124; 审批与安全 &#124; 本轮发现一个只读 profile bug，已修复；越界读、未审批 exec、只读写入现在都能挡住 &#124; sandbox/approval 模型更成熟，但 `--full-auto` 下仍会遇到 Windows 权限/文件锁问题 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 141 | <code>&#124; transcript / event &#124; AILIS 有 `tool.call` / `tool.result` JSONL 和 audit，简单可验收 &#124; Codex 有 `item.started/completed`、`turn.completed` 等更正式协议；复杂任务未正常完成 turn &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 142 | <code>&#124; 可靠性 &#124; 当前本地工具链更稳定，适合个人桌面任务 &#124; 模型能力强，但依赖服务端、网络、CLI 版本和流式连接稳定性 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 143 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 144 | <code>## 判断</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 145 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 146 | <code>AILIS 当前更像“可靠的本地工具执行器 + 初版 Agent Loop”：速度快、确定性强、便于验收，但复杂任务的自主修复能力还不如 Codex。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 147 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 148 | <code>Codex 更像“完整工程智能体 runtime”：会自己观察、修复、换策略，事件协议也更成熟；但这轮本机实测里，服务端重连、CLI 版本、Windows 文件操作让它的端到端稳定性并不完美。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 149 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 150 | <code>对 AILIS 下一步最有价值的是补三块：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 151 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 152 | <code>1. 让 Agentic Executor 在工具失败后自动换策略，而不是依赖固定脚本。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 153 | <code>2. 把 `apply_patch` / 精确编辑工具做成一等工具，减少整文件覆盖。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 154 | <code>3. 给长任务加 `turn.completed` 级别的最终状态和失败收敛，不只留 tool transcript。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
