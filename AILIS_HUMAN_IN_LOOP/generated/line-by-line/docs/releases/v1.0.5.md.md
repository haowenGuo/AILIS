# docs/releases/v1.0.5.md 逐行讲解

- 快照提交：`a1f34b0c24bbf5fce436670e26ced97da3cb72c7`
- 文件职责：设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。
- 文件类型：`documentation`
- 原始行数：40
- SHA-256：`ab2c0a1f624c5530e47adf7aa44b3f1b61e9f1583fc0778e47bec85b400cbd89`
- 可运行副本：[打开源文件](../../../../source/docs/releases/v1.0.5.md)
- 依赖：未从静态文本识别到显式依赖
- 主要符号：未从静态文本识别到命名符号

> 阅读方法：先理解文件职责与依赖，再按行阅读；涉及异步、权限、记忆、工具和外部 I/O 的行应继续追踪调用方、错误路径和测试。

| 行号 | 原代码 | 逐行说明 |
| ---: | --- | --- |
| 1 | <code># AILIS v1.0.5 Release Notes</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 2 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 3 | <code>Release date: 2026-06-22</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 4 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 5 | <code>## Highlights</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 6 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 7 | <code>- Renamed the desktop assistant/tooling surface toward AILIS across runtime code, docs, tests, and UI copy.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 8 | <code>- Added Codex-style deferred tool discovery so the model starts with a small core tool surface and exposes MCP/Web/research tools on demand.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 9 | <code>- Hardened tool schema contracts for required parameters, direct MCP calls, and invalid/empty tool arguments.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 10 | <code>- Improved Web/Search evidence handling with evidence-gap reporting, LLM evidence audit requirements, Crawl4AI-style rendered fetch fallback, and local bundled web runtime preparation.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 11 | <code>- Added GAIA-derived repair and spend-safety loops to reduce runaway iterations and make failed task chains easier to analyze.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 12 | <code>- Added memory quality improvements, including larger relevant-memory context and control-panel memory clearing.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 13 | <code>- Added local model provider support paths for OpenAI-compatible APIs, vLLM, Ollama-oriented deployment, and one-click local model setup guidance.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 14 | <code>- Improved voice/TTS defaults so release builds do not unexpectedly speak malformed action/emote text; CosyVoice3 now uses an explicit local runtime bootstrap path.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 15 | <code>- Added AILIS app icons, tray assets, and favicon resources for desktop and web presentation.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 16 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 17 | <code>## Release Gate</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 18 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 19 | <code>Validation completed before publishing:</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 20 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 21 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 22 | <code>pnpm test:desktop-llm</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 23 | <code>pnpm test:ailis-agent-execution-flow</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 24 | <code>node --test tests/mcp-ailis-research-server.test.mjs</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 25 | <code>pnpm test:ailis-gateway</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 26 | <code>pnpm build</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 27 | <code>pnpm exec electron-builder --config electron-builder.yml --win portable --x64</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 28 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 29 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 30 | <code>Windows artifact:</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 31 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 32 | <code>- `AILIS-Portable-1.0.5-win-x64.exe`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 33 | <code>- SHA256: `9EA074DD04794D5F52A05254708A35625F78ECEABDA067A477B0983230E09671`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 34 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 35 | <code>## Known Boundaries</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 36 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 37 | <code>- Vision screenshot understanding still requires a vision-capable provider/model. Text-only providers can capture screenshots but cannot run `describe_image`.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 38 | <code>- Web/Search quality is much better structured than earlier builds, but high-confidence answers should still depend on fetched/parsed evidence instead of search-result snippets alone.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 39 | <code>- GAIA auto-optimization is intentionally safety-gated; it should not be run as an unlimited API-spend loop.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 40 | <code>- The v1.0.5 Windows release publishes the portable build. A full NSIS installer should only be re-enabled after the installer pipeline produces a complete non-stub executable.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
