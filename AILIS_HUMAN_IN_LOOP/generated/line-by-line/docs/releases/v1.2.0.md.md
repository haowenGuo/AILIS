# docs/releases/v1.2.0.md 逐行讲解

- 快照提交：`a1f34b0c24bbf5fce436670e26ced97da3cb72c7`
- 文件职责：设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。
- 文件类型：`documentation`
- 原始行数：43
- SHA-256：`6aad3bc16665feba7a8fbf1f706c033541cf58f3286602f7bae1148249556d57`
- 可运行副本：[打开源文件](../../../../source/docs/releases/v1.2.0.md)
- 依赖：未从静态文本识别到显式依赖
- 主要符号：`calls`

> 阅读方法：先理解文件职责与依赖，再按行阅读；涉及异步、权限、记忆、工具和外部 I/O 的行应继续追踪调用方、错误路径和测试。

| 行号 | 原代码 | 逐行说明 |
| ---: | --- | --- |
| 1 | <code># AILIS v1.2.0 Release Notes</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 2 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 3 | <code>Release date: 2026-07-20</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 4 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 5 | <code>## Highlights</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 6 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 7 | <code>- Reworked the desktop Agent architecture around a user-facing Persona and a persistent system TaskAgent, with immutable task handoff, durable checkpoints, compact task-result packets, and clearer separation between relationship context and execution context.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 8 | <code>- Aligned the model-visible runtime more closely with the OpenAI Responses and Codex object model, including ordered response items, native function calls and outputs, tool discovery, parallel tool calls, context management, and compaction records.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 9 | <code>- Improved long-running task execution with explicit context lanes, durable evidence and output references, bounded finalization, richer transcript diagnostics, and safer recovery from invalid tool calls or provider timeouts.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 10 | <code>- Added persistent desktop chat history, restart recovery, raw-memory curation, user and relationship capsules, and structured Persona context budgets.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 11 | <code>- Rebuilt proactive companionship around the ordinary conversation context: companion mode wakes the Persona on a fixed heartbeat, while work mode keeps a separate value judgment before reporting.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 12 | <code>- Expanded Artifact Tools across XLSX, DOCX, PPTX, PDF, image, text, and archive workflows, including indexed inspection, search, query, rendering, validation, roundtrip checks, and materialized workbench data for code-based reasoning.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 13 | <code>- Upgraded web research to Codex-style search and source viewports, preserving opened-source evidence across TaskAgent handoff while keeping search ranking metadata separate from answer evidence.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 14 | <code>- Added optional EMBER-Harness stage checks, ToolSandbox shadow evaluation, Agent Lab observability, and refreshed GAIA, SWE-bench, and benchmark regression infrastructure.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 15 | <code>- Refreshed the official homepage and multilingual project documentation with current architecture and benchmark snapshots.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 16 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 17 | <code>## Packaging</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 18 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 19 | <code>AILIS v1.2.0 continues to use the lightweight core package strategy. Large Python, CosyVoice3, ASR, and Web/Search runtimes are not bundled into the default installer and remain optional runtime components.</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 20 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 21 | <code>The Windows binaries are not Authenticode signed. Windows SmartScreen may therefore display an unknown-publisher warning.</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 22 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 23 | <code>## Windows Artifacts</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 24 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 25 | <code>- `AILIS-Setup-1.2.0-win-x64.exe`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 26 | <code>  - Size: `429,342,105 bytes`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 27 | <code>  - SHA256: `BD6A43F8E0791B9E5384734015EAC95707F2149E08A42660294E5341B66EDFFF`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 28 | <code>- `AILIS-Portable-1.2.0-win-x64.exe`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 29 | <code>  - Size: `428,986,315 bytes`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 30 | <code>  - SHA256: `A1A5F01F34B85ED3092028F6419CF405730D664F70F92E95D020BC69265C9EE3`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 31 | <code>- `AILIS-Setup-1.2.0-win-x64.exe.blockmap`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 32 | <code>  - Size: `440,007 bytes`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 33 | <code>  - SHA256: `3FADAFB42BB3EF9A9C694F8D17415753BCAADF25A0314E1884CAAB098B73DCA9`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 34 | <code>- `AILIS-Release-core-1.2.0.json`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 35 | <code>  - Size: `1,468 bytes`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 36 | <code>  - SHA256: `8A99E574E0E4CD765AD4978C10C4B0C2B5A6F11EF36D1085710EB6AD366FF9C3`</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 37 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 38 | <code>## Release Validation</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“设计与运维文档：记录该主题的架构、决策、验证证据和使用方法。”这一文件职责。 |
| 39 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 40 | <code>- Release plan and production Vite build completed.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 41 | <code>- Windows NSIS installer and portable executable completed.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 42 | <code>- Agent Runner, LLM Planner, Memory, Gateway, Artifact Tools, Persona Renderer, proactive companionship, desktop chat history, TaskAgent Harness, Codex Bridge, and SWE-bench setup/runtime tests passed.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 43 | <code>- Package version, executable version, artifact sizes, and SHA256 checksums were verified after the build.</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
