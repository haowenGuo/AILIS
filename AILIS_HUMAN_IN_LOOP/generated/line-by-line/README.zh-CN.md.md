# README.zh-CN.md 逐行讲解

- 快照提交：`a1f34b0c24bbf5fce436670e26ced97da3cb72c7`
- 文件职责：AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。
- 文件类型：`documentation`
- 原始行数：290
- SHA-256：`caad24ff2507d203b09c40c1329ab988d6fcf4126cb6d2596a4f0ddffc11c2c1`
- 可运行副本：[打开源文件](../../source/README.zh-CN.md)
- 依赖：未从静态文本识别到显式依赖
- 主要符号：未从静态文本识别到命名符号

> 阅读方法：先理解文件职责与依赖，再按行阅读；涉及异步、权限、记忆、工具和外部 I/O 的行应继续追踪调用方、错误路径和测试。

| 行号 | 原代码 | 逐行说明 |
| ---: | --- | --- |
| 1 | <code>&lt;div align="center"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 2 | <code>  &lt;h1&gt;AILIS Assistant&lt;/h1&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 3 | <code>  &lt;p&gt;&lt;strong&gt;开源桌面具身 AI 助手：集成 VRM 角色、实时语音、视觉上下文、记忆系统，以及接近 Codex 工作方式的 Agent Harness。&lt;/strong&gt;&lt;/p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 4 | <code>  &lt;p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 5 | <code>    &lt;img alt="Version" src="https://img.shields.io/badge/version-1.1.0-2563eb?style=for-the-badge"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 6 | <code>    &lt;img alt="Runtime" src="https://img.shields.io/badge/runtime-Electron-0f172a?style=for-the-badge"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 7 | <code>    &lt;img alt="License" src="https://img.shields.io/badge/license-MIT-059669?style=for-the-badge"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 8 | <code>  &lt;/p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 9 | <code>  &lt;p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 10 | <code>    &lt;img alt="ToolSandbox 冻结 holdout 均值 71.51%" src="https://img.shields.io/badge/ToolSandbox_holdout-71.51%25-2563eb?style=for-the-badge"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 11 | <code>    &lt;img alt="GAIA Level 1 严格协议第一轮 77.36%" src="https://img.shields.io/badge/GAIA_L1_strict_run_1-77.36%25-059669?style=for-the-badge"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 12 | <code>    &lt;img alt="内部长期陪伴评测 78.46 分" src="https://img.shields.io/badge/Humanlike_longitudinal-78.46%2F100-059669?style=for-the-badge"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 13 | <code>  &lt;/p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 14 | <code>  &lt;p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 15 | <code>    &lt;a href="README.md"&gt;English&lt;/a&gt; ·</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 16 | <code>    &lt;a href="README.zh-CN.md"&gt;简体中文&lt;/a&gt; ·</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 17 | <code>    &lt;a href="README.ja.md"&gt;日本語&lt;/a&gt; ·</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 18 | <code>    &lt;a href="README.ko.md"&gt;한국어&lt;/a&gt; ·</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 19 | <code>    &lt;a href="README.fr.md"&gt;Français&lt;/a&gt; ·</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 20 | <code>    &lt;a href="README.de.md"&gt;Deutsch&lt;/a&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 21 | <code>  &lt;/p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 22 | <code>&lt;/div&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 23 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 24 | <code>---</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 25 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 26 | <code>## 评测结果总览</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 27 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 28 | <code>AILIS 不只是角色演示，而是一套持续接受端到端评测的 Agent 系统。当前证据覆盖有状态工具调用、通用助理任务、长期陪伴表现和桌面操作；每个分数都同时公开样本规模与可宣称边界。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 29 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 30 | <code>&#124; 评测方向 &#124; 结果 &#124; 规模 &#124; 证据定位 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 31 | <code>&#124; --- &#124; ---: &#124; ---: &#124; --- &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 32 | <code>&#124; **Apple ToolSandbox** &#124; 冻结 holdout 均值 **71.51%** &#124; 239 / 239 官方评分，0 errors &#124; 当前主要公开任务质量分数 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 33 | <code>&#124; **GAIA Level 1 严格复现** &#124; 第一轮 **41 / 53，77.36%** &#124; 规定两轮中的第一轮 &#124; 严格逐题记忆隔离的暂定结果；第二轮待完成 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 34 | <code>&#124; **GAIA Level 1 历史结果** &#124; 两轮均值 **85.85%**；最佳单轮 **90.57%** &#124; 53 道公开 validation 题 x 2 &#124; 历史本地诊断；当时缺少逐题记忆隔离 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 35 | <code>&#124; **长期陪伴评测** &#124; 加权均值 **78.46 / 100** &#124; 30 天场景中的 171 个 Judge checkpoint &#124; 内部产品质量评测 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 36 | <code>&#124; **OSWorld 小样本** &#124; **2 / 4，50%** &#124; 4 个历史桌面任务 &#124; 早期外部信号，样本不足以做广泛结论 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 37 | <code>&#124; **人类化数据集校验** &#124; **1000 / 1000** 有效 &#124; 9 类任务、251 个负向探针 &#124; 表示评测覆盖度，不表示模型能力 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 38 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 39 | <code>&gt; **当前主分数：**Apple ToolSandbox 冻结 holdout 均值 **71.51%**。GAIA 严格协议已经完成第一轮，成绩为 **77.36%**，运行中禁用 benchmark memory，并且没有替换失败题；在第二轮 53 题完整结束前，它仍是暂定结果。历史 85.85% 均值继续公开用于透明对照，但不作为当前可复现主张。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 40 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 41 | <code>[完整 Benchmark Scorecard](docs/ailis-demo-benchmark-scorecard.md) ·</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 42 | <code>[GAIA 评测方法](docs/ailis-desktop-real-gaia-eval.md) ·</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 43 | <code>[ToolSandbox 协议与门禁](docs/ailis-toolsandbox-v4-optimization-plan.md)</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 44 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 45 | <code>&lt;p align="center"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 46 | <code>  &lt;img alt="AILIS 评测快照：ToolSandbox 71.51%，GAIA 严格第一轮 77.36%，长期陪伴 78.46 分，OSWorld 小样本 2/4" src="docs/assets/benchmarks/ailis-evaluation-snapshot-20260720.svg"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 47 | <code>&lt;/p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 48 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 49 | <code>## AILIS 是什么</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 50 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 51 | <code>AILIS Assistant 是一个桌面优先的具身 AI 助手项目。它把 3D VRM 角色、Electron 桌面窗口、语音交互、截图视觉上下文、长期记忆，以及结构化 Agent Runtime 放在同一个系统里。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 52 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 53 | <code>它不只是一个网页聊天机器人。AILIS 的目标是成为真正常驻桌面的个人 AI 助手：可以和用户自然交流，在得到许可时理解屏幕上下文，记住有价值的偏好，并通过可审计、可批准的工具完成实际任务。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 54 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 55 | <code>## 项目定位</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 56 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 57 | <code>多数 AI 助手项目容易分成两种：一种有角色表现力，但没有稳定执行能力；另一种有自动化能力，却像开发者控制台。AILIS 希望同时保留两边的优势：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 58 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 59 | <code>- 表层是有存在感、表情、动作、语音和关系感的角色体验。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 60 | <code>- 底层是可规划、可路由、可审批、可记录证据、可恢复的 Agent Harness。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 61 | <code>- 运行时优先本地桌面，让用户自己的设置、记忆、日志和模型配置留在自己机器上。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 62 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 63 | <code>## 当前能力</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 64 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 65 | <code>- VRM 桌面角色，支持表情、动作、口型同步和对话气泡。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 66 | <code>- Electron 桌宠窗口、聊天窗口、控制面板、托盘集成和本地持久化状态。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 67 | <code>- OpenAI 兼容模型提供商配置，支持自定义 base URL 和本地模型工作流。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 68 | <code>- 桌面 TTS worker、云端语音路径和可选本地语音识别 worker。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 69 | <code>- 基于截图、窗口和区域捕获的权限感知视觉上下文。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 70 | <code>- 记忆块、项目上下文、关系状态和轻量反思机制。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 71 | <code>- 文件、代码、电脑操作、邮件、MCP 技能、Web/Search 和本地运行时工具层。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 72 | <code>- 对文件、应用、账号或外部服务有影响的动作使用显式审批模型。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 73 | <code>- 在 Agent 执行链路中接入 EMBER-Harness 阶段门控，对不可信输入、工具调用、工具返回和最终输出进行检查。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 74 | <code>- 人类化体验评测、工具契约测试、Gateway 检查和 Agent 执行烟测。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 75 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 76 | <code>## GAIA：通用 Agent 能力</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 77 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 78 | <code>当前严格逐题记忆隔离协议冻结在提交 `6afc0ae`。第一轮完整成绩为 **41 / 53（77.36%）**；规定的第二轮尚未计入最终均值和稳定率。第一轮在完成 46 行后遇到 Windows 非正常重启，恢复过程沿用同一个 run ID，跳过全部已完成题目，只执行剩余 7 题；没有重试、替换或重新计分任何失败题。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 79 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 80 | <code>&lt;p align="center"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 81 | <code>  &lt;img alt="AILIS GAIA Level 1 validation 历史诊断结果：81.13%、90.57%，均值 85.85%" src="docs/assets/benchmarks/gaia-l1-validation-20260719.svg"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 82 | <code>&lt;/p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 83 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 84 | <code>AILIS 在同一个固定代码提交上，对 GAIA 2023 Level 1 的 53 道公开 validation 题进行了两次完整运行。Codex ChatGPT OAuth bridge 只提供 `gpt-5.5` 大模型，Agent Harness、上下文管理、工具执行和答案出口均由 AILIS 负责。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 85 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 86 | <code>&#124; 指标 &#124; 结果 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 87 | <code>&#124; --- &#124; ---: &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 88 | <code>&#124; 第一次运行 &#124; 43 / 53，**81.13%** &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 89 | <code>&#124; 第二次运行 &#124; 48 / 53，**90.57%** &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 90 | <code>&#124; 两次均值 &#124; 45.5 / 53，**85.85%** &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 91 | <code>&#124; 稳定通过 &#124; 40 / 53 题两次都正确 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 92 | <code>&#124; 结果一致率 &#124; 42 / 53，**79.25%** &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 93 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 94 | <code>这些数字目前只保留为历史诊断结果，不再作为严谨的可复现主成绩。事后审计发现，隔离 workspace 并没有禁用同一轮不同题目之间的持久语义记忆检索，因此存在题间污染风险，不能再称为“独立运行”。评测入口现已显式设置 `memoryPolicy: disabled`；必须在新协议下重新全量运行，才能发布替代主成绩。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 95 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 96 | <code>这属于公开 validation split 上的本地 `desktop-real` 可见答案评测，不是提交到私有 93 题 Level 1 test 排行榜的官方成绩。两轮都使用提交 `4f8f435`、独立 run ID 和隔离 workspace，并禁止 resume、逐题重试、失败题替换和成绩合并，但当时没有严格隔离逐题记忆。详细口径见 [GAIA 评测方法](docs/ailis-desktop-real-gaia-eval.md) 与 [Benchmark Scorecard](docs/ailis-demo-benchmark-scorecard.md)。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 97 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 98 | <code>## ToolSandbox：有状态工具执行</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 99 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 100 | <code>&lt;p align="center"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 101 | <code>  &lt;img alt="AILIS Apple ToolSandbox 离线评测：728/728 个非 RapidAPI 场景完成认证，冻结 holdout 均值 71.51%，定向修复均值 81.49%，稳定性样本均值 88.31%" src="docs/assets/benchmarks/apple-toolsandbox-offline-20260719.svg"&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 102 | <code>&lt;/p&gt;</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 103 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 104 | <code>AILIS 通过正式生产 Agent 链路和官方 on-policy user simulator，完成了全部 **728 个非 RapidAPI** Apple ToolSandbox 场景的官方离线评分。其余 304 个依赖 RapidAPI 的场景不调用、不产生费用，也不进入任何指标。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 105 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 106 | <code>ToolSandbox 为每个场景返回 `0` 到 `1` 的连续相似度分数，反映任务里程碑完成度和 minefield 规避情况。因此，它更适合被理解为“任务质量分”，而不是简单的二元成功率。这里同时公开多个口径，避免用一个数字掩盖差异：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 107 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 108 | <code>&#124; 证据口径 &#124; 结果 &#124; 正确解释 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 109 | <code>&#124; --- &#124; ---: &#124; --- &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 110 | <code>&#124; 认证覆盖率 &#124; **728 / 728，100%** &#124; 表示评测完整性，不等于任务准确率 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 111 | <code>&#124; 冻结 v3 主 holdout &#124; **71.51%** 均值，239 / 239 有效，0 errors &#124; 当前冻结源码的主要泛化估计 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 112 | <code>&#124; Holdout 非零率 / 满分率 &#124; **81.17% / 38.08%** &#124; 239 个场景中 194 个非零、91 个满分 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 113 | <code>&#124; 定向修复 &#124; **81.49%** 均值，155 / 155 有效，0 零分 &#124; 已分析失败队列的诊断结果，不是无偏全局分数 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 114 | <code>&#124; 稳定性样本 &#124; **75.01% -&gt; 88.31%**，配对 **+13.29 个百分点** &#124; 64 个原正分场景的独立无重大回退证据 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 115 | <code>&#124; 稳定性结果 &#124; 29 提升 / 22 持平 / 13 回退 &#124; 含 2 个严重回退；全部预注册门禁通过 &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 116 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 117 | <code>对外最应采用的任务质量主分数是冻结 holdout 均值 **71.51%**。由于 v3 和稳定性 primary batch 都是 0 errors，`valid-only` 与 `errors-as-zero` 完全相同。定向修复与稳定性均值回答的是不同问题，不能与 holdout 分数求平均，也不能宣称为随机因果提升。V1、V2、raw 中间结果、跨漂移和 quarantine 结果均不进入主结论。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 118 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 119 | <code>## 复现与审计</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 120 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 121 | <code>仓库会分别保存 benchmark 计划、逐题结果、进度流、审计事件、完整 transcript 和可读报告。GAIA 回归准入要求 baseline 和 candidate 各自至少完成两次相同任务集的全量运行：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 122 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 123 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 124 | <code>pnpm bench:gaia:desktop-real:smoke</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 125 | <code>pnpm bench:gaia:desktop-real:l1</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 126 | <code>pnpm bench:gaia:compare -- \</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 127 | <code>  --baseline baseline-run-1.jsonl \</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 128 | <code>  --baseline baseline-run-2.jsonl \</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 129 | <code>  --candidate candidate-run-1.jsonl \</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 130 | <code>  --candidate candidate-run-2.jsonl \</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 131 | <code>  --expected-tasks 53 \</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 132 | <code>  --output eval-results/engineering/gaia-regression-gate.md</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 133 | <code>pnpm eval:ailis-humanlike:longitudinal-agent:validate</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 134 | <code>pnpm bench:osworld:readiness</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 135 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 136 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 137 | <code>默认 GAIA 门禁会拒绝任务缺失或替换、可见成功率下降、超时增加、P95 延迟增加超过 15%、平均 token 增加超过 10%，以及稳定正确题变为稳定错误。针对 benchmark 答案编写专用硬路由不属于可接受优化。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 138 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 139 | <code>## 架构概览</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 140 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 141 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 142 | <code>用户 / 语音 / 屏幕</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 143 | <code>        &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 144 | <code>        v</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 145 | <code>AILIS 桌面 UI</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 146 | <code>  - VRM 角色</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 147 | <code>  - 聊天窗口</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 148 | <code>  - 控制面板</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 149 | <code>        &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 150 | <code>        v</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 151 | <code>Agent Harness</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 152 | <code>  - 规划器</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 153 | <code>  - 工具路由</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 154 | <code>  - 审批门禁</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 155 | <code>  - EMBER-Harness 阶段门控</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 156 | <code>  - 证据日志</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 157 | <code>  - 恢复循环</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 158 | <code>        &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 159 | <code>        v</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 160 | <code>运行时服务</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 161 | <code>  - 模型提供商</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 162 | <code>  - 语音 / ASR / TTS</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 163 | <code>  - 视觉捕获</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 164 | <code>  - 记忆存储</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 165 | <code>  - 本地工具 / MCP</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 166 | <code>        &#124;</code> | Markdown 表格行：以列结构表达对照关系、字段定义或证据。 |
| 167 | <code>        v</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 168 | <code>验证体系</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 169 | <code>  - 测试</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 170 | <code>  - 评测</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 171 | <code>  - 烟测</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 172 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 173 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 174 | <code>## EMBER-Harness 阶段门控</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 175 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 176 | <code>AILIS 中接入了 EMBER-Harness，用于在智能体执行链路中做阶段级安全控制。它不是只在最后回答处做一次检查，而是在风险内容可能进入或传播的关键边界进行检查：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 177 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 178 | <code>- 用户输入进入 Agent 上下文之前。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 179 | <code>- 工具调用执行之前，尤其是可能产生副作用的操作。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 180 | <code>- 工具返回结果重新进入模型上下文之前。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 181 | <code>- 最终回答展示给用户之前。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 182 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 183 | <code>每次检查都会形成可审计记录，包括阶段名称、边界位置、快照哈希、估算词元数、检查决策和可回退的稳定快照。控制面板可将本地安全判定设为“关闭 / 仅观察 / 启用拦截”；关闭时不会加载或下载模型。默认判定器使用量化多语言 DistilBERT ONNX 模型在本机运行，不调用生成式大模型，首次启用约下载 136 MB，缓存保存在持久化 AILIS 状态目录。该轻量模型适合识别显性毒性和仇恨风险，但不能替代细粒度的隐性偏见或刻板印象推理器。也可通过 `AILIS_EMBER_HARNESS`、`AILIS_EMBER_HARNESS_MODE`、`AILIS_EMBER_SAFETY_MODEL` 和阈值环境变量覆盖部署配置；运行状态可通过 `/ember-harness/status` 查看。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 184 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 185 | <code>## 仓库结构</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 186 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 187 | <code>```text</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 188 | <code>electron/   Electron 主进程、预加载桥、本地运行时服务和工具适配器</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 189 | <code>src/        桌宠、聊天、控制面板、语音、视觉 UI、气泡等渲染端应用</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 190 | <code>backend/    可选 FastAPI 后端、API schema、记忆服务和静态资源</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 191 | <code>Resources/  VRM 模型、VRMA 动作、参考音频和角色资源</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 192 | <code>docs/       架构、记忆、工具生态、评测和发布规划文档</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 193 | <code>evals/      人类化体验场景和长期陪伴评测数据</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 194 | <code>scripts/    运行时准备、验证、烟测、基准测试和打包脚本</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 195 | <code>tests/      Runtime、Memory、Tools、Contracts、Gateway、Agent 等测试</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 196 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 197 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 198 | <code>## 快速启动</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 199 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 200 | <code>安装依赖：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 201 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 202 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 203 | <code>pnpm install</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 204 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 205 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 206 | <code>以开发模式启动桌面端：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 207 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 208 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 209 | <code>pnpm desktop:dev</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 210 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 211 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 212 | <code>构建并启动桌面端：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 213 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 214 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 215 | <code>pnpm desktop:start</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 216 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 217 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 218 | <code>打包 Windows 桌面应用：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 219 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 220 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 221 | <code>pnpm desktop:package</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 222 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 223 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 224 | <code>可选后端启动：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 225 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 226 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 227 | <code>python -m venv .venv</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 228 | <code>.venv\Scripts\activate</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 229 | <code>pip install -r requirements.txt</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 230 | <code>copy backend\.env.example backend\.env</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 231 | <code>python -m uvicorn backend.main:app --reload</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 232 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 233 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 234 | <code>## 模型与语音配置</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 235 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 236 | <code>AILIS 在应用层不绑定单一模型供应商。可以通过桌面控制面板或本地环境文件配置：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 237 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 238 | <code>- OpenAI 兼容云端提供商。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 239 | <code>- 本地 vLLM endpoint。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 240 | <code>- Ollama 方向的本地工作流。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 241 | <code>- 自定义 base URL、模型名、请求超时和私有 API Key。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 242 | <code>- 可选本地 ASR 和桌面 TTS 运行时准备。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 243 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 244 | <code>不要把真实 API Key、账号凭证、聊天记录、本地模型缓存、运行日志或生成的评测结果提交到仓库。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 245 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 246 | <code>## 常用命令</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 247 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 248 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 249 | <code>pnpm test:ailis-runtime</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 250 | <code>pnpm test:ailis-agent</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 251 | <code>pnpm test:ailis-tool-contracts</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 252 | <code>pnpm test:ailis-memory</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 253 | <code>pnpm ailis:validate-harness</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 254 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 255 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 256 | <code>完整 Gateway 验证较重，会运行更多 Runtime、契约、工具、记忆、Agent 和烟测检查：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 257 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 258 | <code>```bash</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 259 | <code>pnpm ailis:validate-gateway</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 260 | <code>```</code> | Markdown 代码围栏：开始或结束一段保持原格式的代码/命令示例。 |
| 261 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 262 | <code>## 核心文档</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 263 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 264 | <code>- [完整文档导航](docs/README.md)</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 265 | <code>- [具身 Agent 架构](docs/ailis-embodied-agent-architecture.md)</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 266 | <code>- [System TaskAgent 架构](docs/ailis-system-taskagent-architecture.md)</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 267 | <code>- [Codex 多 Agent 数据流迁移](docs/ailis-codex-multi-agent-dataflow-migration.md)</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 268 | <code>- [记忆架构 V2](docs/ailis-memory-architecture-v2.md)</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 269 | <code>- [人类化体验评测](docs/ailis-humanlike-eval.md)</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 270 | <code>- [Benchmark 总览与 Scorecard](docs/ailis-demo-benchmark-scorecard.md)</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 271 | <code>- [工具生态驱动指南](docs/tool-ecosystem-driver-guide.md)</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 272 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 273 | <code>## 项目状态</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 274 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 275 | <code>当前发布线：`v1.1.0`。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 276 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 277 | <code>AILIS 正在积极开发。它已经具备较完整的桌面运行时、Agent Harness、工具层和评测面，但仍应被视为 alpha 阶段产品/运行时，而不是生产级 Agent OS。近期重点是可靠性：更清晰的工具契约、更安全的审批、更稳定的记忆行为、更顺滑的本地模型配置，以及更高质量的端到端评测。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 278 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 279 | <code>## 隐私与安全</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 280 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 281 | <code>AILIS 面向个人桌面使用，所以隐私和控制是架构的一部分：</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
| 282 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 283 | <code>- 视觉捕获需要权限意识，目的是理解上下文，不是静默操作。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 284 | <code>- 会影响文件、应用、账号或外部服务的动作应经过显式审批。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 285 | <code>- 本地记忆和运行时状态默认留在用户机器上。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 286 | <code>- 密钥应放在本地配置中，绝不能进入源码仓库。</code> | Markdown 列表项：列出该主题下的一个事实、步骤、约束或结论。 |
| 287 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 288 | <code>## 开源许可</code> | 注释：记录作者意图、约束、分段说明或维护提示；不会直接执行。 本行属于“AILIS 仓库组成文件；需结合目录、引用关系和逐行讲解理解其职责。”这一文件职责。 |
| 289 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 290 | <code>AILIS 源代码采用 [MIT License](LICENSE) 开源。部分随包资源、第三方模型、动作和语音资源可能有独立许可；重新分发前请确认对应资源说明。</code> | 文档正文：解释设计意图、操作方法、证据边界或维护约定。 |
