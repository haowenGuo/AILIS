# backend/services/blog_service.py 逐行讲解

- 快照提交：`a1f34b0c24bbf5fce436670e26ced97da3cb72c7`
- 文件职责：后端服务层：实现模型、记忆、聊天或业务服务逻辑。
- 文件类型：`source-code`
- 原始行数：177
- SHA-256：`9c2c0d0be2874f1643425c98f48075c31ec839bf1f8b5247aea135fc4eca7291`
- 可运行副本：[打开源文件](../../../../source/backend/services/blog_service.py)
- 依赖：`json`、`dataclasses`、`functools`、`pathlib`、`typing`、`markdown`
- 主要符号：`FeaturedProject`、`AboutSection`、`InspirationLink`、`BlogSite`、`BlogPost`、`_resolve_locale`、`_load_site`、`_render_markdown`、`_load_posts`、`get_blog_site`、`get_blog_posts`、`get_blog_post`、`get_featured_posts`

> 阅读方法：先理解文件职责与依赖，再按行阅读；涉及异步、权限、记忆、工具和外部 I/O 的行应继续追踪调用方、错误路径和测试。

| 行号 | 原代码 | 逐行说明 |
| ---: | --- | --- |
| 1 | <code>import json</code> | 导入 Python 依赖 `json`，供本模块调用其类型、函数或常量。 |
| 2 | <code>from dataclasses import dataclass</code> | 导入 Python 依赖 `dataclasses`，供本模块调用其类型、函数或常量。 |
| 3 | <code>from functools import lru_cache</code> | 导入 Python 依赖 `functools`，供本模块调用其类型、函数或常量。 |
| 4 | <code>from pathlib import Path</code> | 导入 Python 依赖 `pathlib`，供本模块调用其类型、函数或常量。 |
| 5 | <code>from typing import Any</code> | 导入 Python 依赖 `typing`，供本模块调用其类型、函数或常量。 |
| 6 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 7 | <code>import markdown</code> | 导入 Python 依赖 `markdown`，供本模块调用其类型、函数或常量。 |
| 8 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 9 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 10 | <code>BLOG_DIR = Path(__file__).resolve().parent.parent / "blog_content"</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 11 | <code>POSTS_PATH = BLOG_DIR / "posts.json"</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 12 | <code>SITE_PATH = BLOG_DIR / "site.json"</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 13 | <code>POSTS_DIR = BLOG_DIR / "posts"</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 14 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 15 | <code>SUPPORTED_LOCALES = {"zh", "en"}</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 16 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 17 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 18 | <code>@dataclass(frozen=True)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 19 | <code>class FeaturedProject:</code> | 定义 Python 类 `FeaturedProject`，封装相关状态、协议和方法。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 20 | <code>    name: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 21 | <code>    description: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 22 | <code>    link: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 23 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 24 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 25 | <code>@dataclass(frozen=True)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 26 | <code>class AboutSection:</code> | 定义 Python 类 `AboutSection`，封装相关状态、协议和方法。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 27 | <code>    title: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 28 | <code>    body: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 29 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 30 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 31 | <code>@dataclass(frozen=True)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 32 | <code>class InspirationLink:</code> | 定义 Python 类 `InspirationLink`，封装相关状态、协议和方法。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 33 | <code>    name: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 34 | <code>    url: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 35 | <code>    note: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 36 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 37 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 38 | <code>@dataclass(frozen=True)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 39 | <code>class BlogSite:</code> | 定义 Python 类 `BlogSite`，封装相关状态、协议和方法。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 40 | <code>    locale: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 41 | <code>    site_title: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 42 | <code>    site_subtitle: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 43 | <code>    hero_title: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 44 | <code>    hero_intro: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 45 | <code>    bio: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 46 | <code>    location: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 47 | <code>    email: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 48 | <code>    github: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 49 | <code>    x: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 50 | <code>    now_title: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 51 | <code>    now_text: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 52 | <code>    nav: dict[str, str]</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 53 | <code>    labels: dict[str, str]</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 54 | <code>    about_sections: list[AboutSection]</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 55 | <code>    featured_projects: list[FeaturedProject]</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 56 | <code>    inspirations: list[InspirationLink]</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 57 | <code>    projects_intro: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 58 | <code>    writing_intro: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 59 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 60 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 61 | <code>@dataclass(frozen=True)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 62 | <code>class BlogPost:</code> | 定义 Python 类 `BlogPost`，封装相关状态、协议和方法。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 63 | <code>    locale: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 64 | <code>    slug: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 65 | <code>    title: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 66 | <code>    summary: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 67 | <code>    published_at: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 68 | <code>    reading_time: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 69 | <code>    featured: bool</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 70 | <code>    tags: list[str]</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 71 | <code>    body_html: str</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 72 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 73 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 74 | <code>def _resolve_locale(locale: str &#124; None) -&gt; str:</code> | 定义 Python 函数 `_resolve_locale`；其缩进块实现具体业务或工具行为。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 75 | <code>    normalized = (locale or "zh").strip().lower()</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 76 | <code>    return normalized if normalized in SUPPORTED_LOCALES else "zh"</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
| 77 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 78 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 79 | <code>def _load_site(locale: str) -&gt; BlogSite:</code> | 定义 Python 函数 `_load_site`；其缩进块实现具体业务或工具行为。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 80 | <code>    with SITE_PATH.open("r", encoding="utf-8") as file:</code> | 开始赋值或复合结构，后续行将补充其字段、元素或实现。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 81 | <code>        raw_site = json.load(file)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 82 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 83 | <code>    locale_key = _resolve_locale(locale)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 84 | <code>    localized = raw_site["locales"][locale_key]</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 85 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 86 | <code>    return BlogSite(</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
| 87 | <code>        locale=locale_key,</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 88 | <code>        site_title=str(localized["site_title"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 89 | <code>        site_subtitle=str(localized["site_subtitle"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 90 | <code>        hero_title=str(localized["hero_title"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 91 | <code>        hero_intro=str(localized["hero_intro"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 92 | <code>        bio=str(localized["bio"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 93 | <code>        location=str(localized["location"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 94 | <code>        email=str(localized["email"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 95 | <code>        github=str(localized["github"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 96 | <code>        x=str(localized.get("x", "")),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 97 | <code>        now_title=str(localized["now_title"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 98 | <code>        now_text=str(localized["now_text"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 99 | <code>        nav=dict(localized["nav"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 100 | <code>        labels=dict(localized["labels"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 101 | <code>        about_sections=[</code> | 开始赋值或复合结构，后续行将补充其字段、元素或实现。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 102 | <code>            AboutSection(title=str(item["title"]), body=str(item["body"]))</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 103 | <code>            for item in localized.get("about_sections", [])</code> | Python 循环：按集合元素或条件重复执行后续缩进块。 |
| 104 | <code>        ],</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 105 | <code>        featured_projects=[</code> | 开始赋值或复合结构，后续行将补充其字段、元素或实现。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 106 | <code>            FeaturedProject(</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 107 | <code>                name=str(item["name"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 108 | <code>                description=str(item["description"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 109 | <code>                link=str(item["link"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 110 | <code>            )</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 111 | <code>            for item in localized.get("featured_projects", [])</code> | Python 循环：按集合元素或条件重复执行后续缩进块。 |
| 112 | <code>        ],</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 113 | <code>        inspirations=[</code> | 开始赋值或复合结构，后续行将补充其字段、元素或实现。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 114 | <code>            InspirationLink(</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 115 | <code>                name=str(item["name"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 116 | <code>                url=str(item["url"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 117 | <code>                note=str(item["note"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 118 | <code>            )</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 119 | <code>            for item in localized.get("inspirations", [])</code> | Python 循环：按集合元素或条件重复执行后续缩进块。 |
| 120 | <code>        ],</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 121 | <code>        projects_intro=str(localized["projects_intro"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 122 | <code>        writing_intro=str(localized["writing_intro"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 123 | <code>    )</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 124 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 125 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 126 | <code>def _render_markdown(body_file: str) -&gt; str:</code> | 定义 Python 函数 `_render_markdown`；其缩进块实现具体业务或工具行为。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 127 | <code>    body_path = BLOG_DIR / body_file</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 128 | <code>    raw_text = body_path.read_text(encoding="utf-8")</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 129 | <code>    return markdown.markdown(</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
| 130 | <code>        raw_text,</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 131 | <code>        extensions=["extra", "sane_lists", "smarty"],</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 132 | <code>    )</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 133 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 134 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 135 | <code>def _load_posts(locale: str) -&gt; list[BlogPost]:</code> | 定义 Python 函数 `_load_posts`；其缩进块实现具体业务或工具行为。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 136 | <code>    with POSTS_PATH.open("r", encoding="utf-8") as file:</code> | 开始赋值或复合结构，后续行将补充其字段、元素或实现。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 137 | <code>        raw_posts = json.load(file)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 138 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 139 | <code>    locale_key = _resolve_locale(locale)</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 140 | <code>    default_locale = "zh"</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 141 | <code>    return [</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
| 142 | <code>        BlogPost(</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 143 | <code>            locale=locale_key,</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 144 | <code>            slug=str(item["slug"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 145 | <code>            title=str((item.get("translations", {}).get(locale_key) or item.get("translations", {}).get(default_locale))["title"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 146 | <code>            summary=str((item.get("translations", {}).get(locale_key) or item.get("translations", {}).get(default_locale))["summary"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 147 | <code>            published_at=str(item["published_at"]),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 148 | <code>            reading_time=str(item.get("reading_time", "")),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 149 | <code>            featured=bool(item.get("featured", False)),</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 150 | <code>            tags=[str(tag) for tag in item.get("tags", [])],</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 151 | <code>            body_html=_render_markdown(</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 152 | <code>                str((item.get("translations", {}).get(locale_key) or item.get("translations", {}).get(default_locale))["body_file"])</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 153 | <code>            ),</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 154 | <code>        )</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 155 | <code>        for item in raw_posts</code> | Python 循环：按集合元素或条件重复执行后续缩进块。 |
| 156 | <code>    ]</code> | 结束当前表达式、调用、数组、对象或代码块，使语法结构闭合。 |
| 157 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 158 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 159 | <code>@lru_cache()</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 160 | <code>def get_blog_site(locale: str = "zh") -&gt; BlogSite:</code> | 定义 Python 函数 `get_blog_site`；其缩进块实现具体业务或工具行为。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 161 | <code>    return _load_site(locale)</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
| 162 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 163 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 164 | <code>@lru_cache()</code> | 执行该文件中的一项具体声明、参数设置、表达式或调用。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 165 | <code>def get_blog_posts(locale: str = "zh") -&gt; list[BlogPost]:</code> | 定义 Python 函数 `get_blog_posts`；其缩进块实现具体业务或工具行为。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 166 | <code>    return _load_posts(locale)</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
| 167 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 168 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 169 | <code>def get_blog_post(slug: str, locale: str = "zh") -&gt; BlogPost &#124; None:</code> | 定义 Python 函数 `get_blog_post`；其缩进块实现具体业务或工具行为。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 170 | <code>    for post in get_blog_posts(locale):</code> | Python 循环：按集合元素或条件重复执行后续缩进块。 |
| 171 | <code>        if post.slug == slug:</code> | Python 条件分支：只有条件成立时才执行后续缩进块。 |
| 172 | <code>            return post</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
| 173 | <code>    return None</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
| 174 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 175 | <code>␠</code> | 空行：分隔相邻语义块，提高可读性；不产生运行时行为。 |
| 176 | <code>def get_featured_posts(locale: str = "zh") -&gt; list[BlogPost]:</code> | 定义 Python 函数 `get_featured_posts`；其缩进块实现具体业务或工具行为。 本行属于“后端服务层：实现模型、记忆、聊天或业务服务逻辑。”这一文件职责。 |
| 177 | <code>    return [post for post in get_blog_posts(locale) if post.featured]</code> | Python 返回语句：结束当前函数并把结果交还调用方。 |
