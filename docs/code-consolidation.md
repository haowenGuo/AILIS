# 代码可达性精简记录

第四轮（2026-09-06）已将生产入口、逐文件依赖台账、独立提取和实际打包验证落地，见 [生产代码提取与精简](production-runtime.md)。下面保留前三轮的历史统计；不要将约 40.6% 的桌面范围拆分误报为新一轮死代码删除量。

日期：2026-09-05。工作树：`F:\AILIS\code-consolidation-20260904`；分支：`codex/code-consolidation-20260904`。未部署、未推送、未替换已安装应用。

## 恢复点与统计口径

完整精简前快照 `1442cc5b54cb7798e46e517fe65a241c366d50c5` 包含当时主仓的未提交源码。第一批提交 `9cc4d731a163c6712eab4a53cdfce97e6d38e993`，第二批提交 `958b5615af22862c243ab2291206e677d9dd40db`。第三批以第二批为修改前基线。不要硬重置主仓已有改动。

| 第一方源码（含测试与脚本的物理行；排除第三方、文档、测试数据） | 文件 | 行数 | 本批净减 |
| --- | ---: | ---: | ---: |
| 完整精简前 | 436 | 249,367 | — |
| 第一批后 | 434 | 247,623 | 1,744 |
| 第二批后 | 429 | 245,325 | 2,298 |
| 第三批后 | 429 | 244,673 | 652 |

累计净减 **4,694 行**。第三批 `electron/`、`src/` 生产 JS 净减 **596 行**，测试净减 56 行；这里含物理空行变化，不等于可执行语句数量。不是把 F 盘所有源码副本相加。

## 删除内容与依据

第一批：移除无人调用的双角色主调度/私有 Persona 草稿链、三个未使用的语音/角色实验模块；当前统一主 Agent、显式兼容任务接口与历史记忆迁移保留。核心架构文档按当前工作树重写，历史设计分开存放。

第二批：

- 删除 48 个 AST 词法绑定无引用、无导出的模块级私有函数（862 行），并复查级联辅助函数。
- 删除未被 HTML/构建加载的 `src/app.js`；保留真实入口 `src/pet-app.js`、`Test/app.js`。
- 删除两个仅供测试使用的两行转发文件；测试迁往 `electron/agent-loop/index.cjs`、`src/rendering/index.js`。
- 删除未被 Gateway 创建的旧 ONNX 分类器；保留敏感词/EMBER 安全链。
- 删除未注册的旧 XLSX reader 和隐藏契约；保留并测试当前 `artifact_tools` 及历史产物查询、计算、缓存和原始载荷读保护。

第三批：

- 删除 30 个无调用方的类方法：旧计划构造/确认结果包装、废弃 Session 投影、重复语音出口、旧 VRM 表情/动作包装、无用记忆/工具/平台别名等。实际审批、流式语音、自动眨眼和结构化角色表现实现保留。
- 删除 21 个函数：无生产消费者的导出、它们的私有辅助函数、旧文本情绪标签转译和未使用的内嵌附件整理函数。其中 `buildTaskRouteDirectToolPrompt`、`renderMaxStepsSurface` 只剩旧测试，连同专属测试退役；角色测试改为验证实际使用的 `surfaceToScreenplay`。
- 删除 14 处独立常量/内置导入声明，以及多绑定导入中的无用属性和局部变量。废弃动作清单、预加载但无人消费的技能目录不再构造；真正按需读取技能的实现不变。
- 保留隐式 `MemoryContext.toJSON` 序列化钩子、故意用于剔除字段的四个解构绑定，以及仍有测试消费者的协议校验/模型输入辅助接口。
- 历史语音方案加上醒目的旧基线说明，指向当前实现，避免把已删方法当作现行链路。

方法不是“跑一次没覆盖就删”：结合 HTML/主进程/worker/CLI 入口图、AST 绑定、跨文件标识符与字符串引用检索、动态调用检查、人工逐项核对以及回归测试。由 FastAPI 挂载的 `backend/static/edu/app.js` 明确保留。

## 验证与限制

- 第一批：196/196 测试通过。第二批：376 项变为 372 项，仍为同名 15 项失败、4 项跳过；没有新增失败。测试减少来自旧分类器测试退役和新增契约检查。
- 第三批 42 组回归：修改前 **498 项 / 479 通过 / 15 失败 / 4 跳过**，修改后 **496 项 / 477 通过 / 15 失败 / 4 跳过**；失败名称集合完全一致，无新增失败。减少的两项只测已退役包装函数；角色测试保留并加强当前结构化出口检查。新增 14 组单独验证为 126/126 通过。
- 既有失败为 `ailis-llm-planner.test.mjs` 的 14 项、`ailis-self-debugger.test.mjs` 的 1 项，未修复或屏蔽。不能称全套测试全绿。
- 首次第三批核心回归曾另有两项 Excel 渲染失败：默认 `C:\Python313\python.exe` 缺少 `openpyxl`。仅在测试进程设置 `AILIS_ARTIFACT_PYTHON`、`AILIS_RAGFLOW_PYTHON` 为已安装的 `C:\Users\Lenovo\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` 后重跑通过相关测试；未安装新依赖或修改全局环境。
- 第三批 34 个改动生产模块：剔除列明删除项后，剩余 AST 与基线一致。导入只删无用属性，不删仍被使用的同组绑定。
- 第三批前后 Vite 构建通过，各 31 个输出文件；JS 合计由 1,313,076 字节降至 1,309,011 字节，减少 **4,065 字节**。第二批的 31 个输出曾完全同 hash；第三批类方法删除会改变产物，不能沿用该结论。
- 扫描 334 个第一方 JS 文件无解析错误；139 个 `electron/src` JS 文件中，无剩余模块级无引用函数声明。本轮类方法候选只剩明确保留的隐式序列化钩子。
- JS 图保留全部 CLI/worker 入口与 11 处动态加载。这不是任意输入、动态插件、Python/IPC 所有分支的完整可达性证明，不能宣称所有剩余代码都必然执行。
- 主仓 `F:\AILIS\main` 原有 32 个文件快照 SHA-256 保持不变。测试生成样例不纳入源码提交。未测新的模型回答质量、缓存率、费用、时延或真实桌面交互，未运行正式 benchmark。

## 本地证据

- [第三批计数、保留 AST、构建与主仓校验](../tmp/code-consolidation-20260905/round3-verification.json)
- [第三批核心回归](../tmp/code-consolidation-20260904/round3-core-verified.json)、[新增 14 组回归](../tmp/code-consolidation-20260904/round3-extra-after.json)
- [第二批核对结果](../tmp/code-consolidation-20260905/verification.json)、[第二批测试基线](../tmp/code-consolidation-20260904/round2-after.json)

审计脚本、精确删除原文和原始测试日志位于本工作树 `tmp/code-consolidation-20260905/` 与 `tmp/code-consolidation-20260904/`，不随发布包分发。源码可通过上述 Git 恢复点找回。
